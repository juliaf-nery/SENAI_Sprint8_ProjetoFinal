export const environment = {
  production: true,
  // ATENÇÃO: 'http://localhost:3001' só existe na sua máquina. Depois de
  // implantar a pasta "login-api" em um serviço com URL pública (Render,
  // Railway, Fly.io, etc.), troque o valor abaixo pela URL pública dela,
  // por exemplo: 'https://sua-login-api.onrender.com'.
  apiUrl: 'http://localhost:3001',
};
